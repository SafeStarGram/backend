version: "3.8"
services:
  redis:
    image: redis:7.2
    container_name: redis_local
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]  # 간단 영속화
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:

